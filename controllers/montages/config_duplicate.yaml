# ===================================
#  Duplicate Behavior Config
# 
# ATTENTION LE TITRE NE DOIT PAS AVOIR D ESPACE

# Model Class name
modelClass: Waka\Cloudis\Models\Montage

name: montages
title: waka.wutils::lang.global.duplicate_the Montage
redirect: backend/waka/cloudis/montages/update/

duplication:
    #Les champs dans le tableau relation seront cloné et attaché au nouveau model 
    # relations:
    #     - quotesdetails

    #Les champs dans le tableau transformation auront les valeurs listé ci dessous 
    # transformation:
    #     quotestatus_id: 1
    #     code: null
    #     unblock: false

    #Les champs qui seront modifié dans le formulaire du popup 
    fields:
        name:
          label: waka.cloudis::lang.montage.name 
        slug:
          label: waka.cloudis::lang.montage.slug
          preset: name

        # _keep:
        #   label: Dupliquer dans ce projet
        #   type: checkbox
        #   default: 0

        # project:
        #   label: Choisissez un projet
        #   type: relation
        #   placeholder: -- choisissez un projet --
        #   select: concat(name,' | ' ,code)
        #   trigger:
        #     action: hide
        #     field: _keep
        #     condition: checked



          
    
        



